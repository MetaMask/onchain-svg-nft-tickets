{
  "globalEnv": ["NEXT_PUBLIC_NETWORK_ID", "NEXT_PUBLIC_INFURA_PROJECT_ID"],
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "lint": {
      "outputs": []
    },
    "local": {
      "cache": false
    },
    "dev": {
      "cache": false
    }
  }
}
